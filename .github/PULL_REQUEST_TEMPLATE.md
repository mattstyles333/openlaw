Thank you for a pull request against Openlaw.

## Kind of change (required)

Pick **one**:

- [ ] **proposal vs law change** — this PR is a proposal (`decisions/proposed/` or `decisions/` with `status: proposed`). It does **not** edit `law/` or `AGENTS.md`.
- [ ] **law change** — this PR edits `law/` and/or `AGENTS.md`. A matching ADR is `status: decided`. Only CODEOWNERS merge. Never auto-merge law.

## What changed

<!-- 1–3 sentences. -->

## Checklist

- [ ] `bash scripts/check-law.sh` exits 0
- [ ] No secrets, tokens, or PII in git
- [ ] I did not auto-merge law
